*.mp4 filter=lfs diff=lfs merge=lfs -text

# Shell scripts should always use LF line endings
*.sh text eol=lf

# Config files that are mounted into Linux containers must use LF
*.conf text eol=lf
*.ini text eol=lf
*.cfg text eol=lf