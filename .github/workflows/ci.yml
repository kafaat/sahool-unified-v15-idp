# Assuming the current content of .github/workflows/ci.yml can be fetched correctly and starting from there:

# ...rest of the file remains unchanged till line 13...
      - name: Install Docker Compose
        run: |
          sudo apt-get update
          sudo apt-get install -y docker-compose
# Following contents remain same after this.
