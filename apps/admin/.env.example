# ═══════════════════════════════════════════════════════════════════════════════
# SAHOOL Admin Dashboard Environment Configuration
# لوحة تحكم سحول - إعدادات البيئة
#
# Copy this file to .env.local and fill in your values:
#   cp .env.example .env.local
# ═══════════════════════════════════════════════════════════════════════════════

# ─────────────────────────────────────────────────────────────────────────────
# API Configuration (Kong Gateway)
# إعدادات API (بوابة Kong)
# ─────────────────────────────────────────────────────────────────────────────
# Backend API URL - points to Kong Gateway
# عنوان API الخلفي - يشير إلى بوابة Kong
NEXT_PUBLIC_API_URL=http://localhost:8000

# WebSocket URL for real-time features (ws-gateway service)
# عنوان WebSocket للميزات الفورية (خدمة ws-gateway)
NEXT_PUBLIC_WS_URL=ws://localhost:8081

# ─────────────────────────────────────────────────────────────────────────────
# Authentication (Server-side only - do not expose to client)
# التوثيق (جانب الخادم فقط - لا تعرضه للعميل)
# ─────────────────────────────────────────────────────────────────────────────
# JWT Secret for token verification - MUST match backend JWT_SECRET_KEY
# مفتاح JWT للتحقق من التوكن - يجب أن يطابق JWT_SECRET_KEY في الخلفية
JWT_SECRET=change_this_jwt_secret_key_at_least_32_characters_long

# JWT Algorithm (default: HS256)
JWT_ALGORITHM=HS256

# ─────────────────────────────────────────────────────────────────────────────
# Session Configuration
# إعدادات الجلسة
# ─────────────────────────────────────────────────────────────────────────────
# Session timeout in minutes (default: 30)
SESSION_TIMEOUT_MINUTES=30

# Token refresh interval in minutes (default: 5)
TOKEN_REFRESH_INTERVAL_MINUTES=5

# ─────────────────────────────────────────────────────────────────────────────
# Security Settings
# إعدادات الأمان
# ─────────────────────────────────────────────────────────────────────────────
# CSRF Secret for cross-site request forgery protection
CSRF_SECRET=change_this_csrf_secret_at_least_32_characters_long

# Allowed origins for CORS (comma-separated)
ALLOWED_ORIGINS=http://localhost:3002,http://localhost:8000

# ─────────────────────────────────────────────────────────────────────────────
# Monitoring & Observability (Optional)
# المراقبة والرصد (اختياري)
# ─────────────────────────────────────────────────────────────────────────────
# Sentry DSN for error tracking
# SENTRY_DSN=https://your-sentry-dsn@sentry.io/project-id

# Enable Sentry in development (default: false)
# SENTRY_ENABLED_DEV=false

# ─────────────────────────────────────────────────────────────────────────────
# Development Settings
# إعدادات التطوير
# ─────────────────────────────────────────────────────────────────────────────
# Node environment
NODE_ENV=development

# Enable debug mode
# DEBUG=true
