# SAHOOL Atmosphere - Analysis Options
# قواعد تحليل الكود للحفاظ على جودة عالية
#
# Run: flutter analyze
# Fix: dart fix --apply
#
# Using flutter_lints for standard linting (CI-compatible)
# https://pub.dev/packages/flutter_lints

include: package:flutter_lints/flutter.yaml

analyzer:
  # Treat missing required params and returns as errors
  errors:
    missing_required_param: error
    missing_return: error
    must_be_immutable: error
    invalid_annotation_target: ignore

  # Exclude generated files
  exclude:
    - "**/*.g.dart"
    - "**/*.freezed.dart"
    - "**/generated/**"
    - "build/**"
    - ".dart_tool/**"

  # Enable stricter type checks
  language:
    strict-casts: true
    strict-inference: true
    strict-raw-types: true

linter:
  rules:
    # Error Prevention - منع الأخطاء
    - avoid_print
    - avoid_returning_null_for_future
    - cancel_subscriptions
    - close_sinks
    - empty_catches
    - avoid_empty_else

    # Type Safety - سلامة الأنواع
    - always_declare_return_types
    - type_annotate_public_apis
    - avoid_types_as_parameter_names
    - prefer_void_to_null

    # Performance - الأداء
    - prefer_const_constructors
    - prefer_const_constructors_in_immutables
    - prefer_const_declarations
    - prefer_const_literals_to_create_immutables
    - prefer_final_locals
    - prefer_final_in_for_each

    # Flutter Specific - خاص بـ Flutter
    - use_key_in_widget_constructors
    - sized_box_for_whitespace
    - avoid_unnecessary_containers
    - sized_box_shrink_expand

    # Style - الأسلوب
    - prefer_single_quotes
    - slash_for_doc_comments

    # Async - التزامن
    - unawaited_futures
    - unnecessary_await_in_return
    - avoid_void_async

    # Null Safety - سلامة null
    - avoid_null_checks_in_equality_operators
    - prefer_null_aware_method_calls
    - prefer_null_aware_operators
    - unnecessary_null_checks

    # Best Practices - أفضل الممارسات
    - only_throw_errors
    - missing_whitespace_between_adjacent_strings
    - no_duplicate_case_values
    - null_closures

# Additional information about this file can be found at
# https://dart.dev/guides/language/analysis-options
