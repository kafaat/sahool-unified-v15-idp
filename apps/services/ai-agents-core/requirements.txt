fastapi>=0.115.0,<1.0.0
uvicorn[standard]>=0.34.0,<1.0.0
pydantic==2.13.1
python-multipart>=0.0.22
httpx>=0.28.1,<1.0.0

# ML/AI Dependencies for Disease Detection - مكتبات التعلم الآلي لاكتشاف الأمراض
numpy>=1.26.0,<2.0.0  # Pin <2.0 to prevent compiled module mismatch with torch/transformers built against 1.x
Pillow>=12.2.0,<13.0.0  # CVE-2026-25990, CVE-2026-40192
opencv-python-headless>=4.9.0,<5.0.0
aiohttp>=3.13.3

# ML Frameworks (at least one required) - أطر عمل التعلم الآلي (مطلوب واحد على الأقل)
# Uncomment based on your preference - قم بإلغاء التعليق بناءً على تفضيلاتك
tensorflow-cpu>=2.20.0  # Aligned with platform constraint (tensorflow-cpu==2.21.0 in constraints.txt)
# torch>=2.0.0  # For PyTorch support
# torchvision>=0.15.0  # For PyTorch vision utilities

structlog>=24.1.0
