# SAHOOL Audit Service Dependencies
# PyJWT required by apps/services/shared/auth/jwt.py (import jwt at module level)
# متطلبات خدمة التدقيق
# Updated: January 2026
# Version: 16.0.0

# Web Framework (matching shared versions)
fastapi>=0.115.0,<1.0.0
starlette>=0.49.1
uvicorn[standard]==0.44.0
pydantic==2.13.1

# HTTP Client
httpx==0.28.1

# JWT Authentication (shared/auth/jwt.py imports jwt at module level)
# CVE-2024-53861 (issuer validation bypass) fixed in 2.10.1
PyJWT>=2.10.1,<3.0.0

# Database & ORM (sync SQLAlchemy - uses psycopg2)
# Note: Tortoise ORM removed - using SQLAlchemy only for consistency
sqlalchemy==2.0.49
alembic==1.18.4
psycopg2-binary==2.9.11

# Async database driver (used by lifespan for connection pool)
asyncpg>=0.30.0,<1.0.0

# NATS Messaging
nats-py>=2.6.0,<3.0.0

# Redis for caching
redis>=7.1.0,<8.0.0

# Utilities
python-dotenv==1.2.2
python-dateutil==2.9.0.post0

# Structured logging
structlog>=24.1.0

# Prometheus metrics — feeds infrastructure/monitoring/prometheus/rules/audit-alerts.yml
# Pinned to match the 17 other services that pin ==0.25.0 (advisory-service,
# billing-core, indicators-service, etc.); range pins make drift-correlated
# incidents harder to reproduce across environments.
prometheus-client==0.25.0

# Testing
pytest==9.0.3
pytest-asyncio==1.3.0
pytest-cov>=6.0.0
