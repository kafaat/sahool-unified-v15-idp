# SAHOOL carbon-service Dependencies
# Updated: April 2026
# Version: 16.0.0
# Purpose: IPCC Tier 1 carbon footprint computation for agriculture.
#
# Package versions are pinned to match the platform-wide manifest used
# by advisory-service / soil-analysis-service / weather-service so the
# centralised license scanner (governance-validation.yml) already has
# them in its allow-list.

# Base requirements (inlined from requirements/base.txt)
fastapi>=0.115.0,<1.0.0
starlette>=0.49.1
uvicorn[standard]==0.42.0
pydantic==2.12.5
pydantic-settings==2.13.1
httpx==0.28.1
python-dotenv==1.2.2

# Testing requirements (inlined from requirements/testing.txt)
pytest==8.4.2
pytest-asyncio==0.26.0
pytest-cov>=6.0.0
pytest-mock==3.15.1

# Service-specific dependencies
nats-py==2.14.0

# Database
asyncpg==0.31.0

# Observability
structlog>=24.1.0

# Authentication (JWT validation)
PyJWT>=2.10.1,<3.0.0  # CVE-2024-53861
cryptography>=44.0.1,<47.0.0  # CVE-2024-12797, CVE-2026-26007

# Prometheus metrics
prometheus-client==0.24.1
