# SAHOOL Shared Authentication Layer Dependencies
# متطلبات طبقة المصادقة المشتركة

# JWT
PyJWT==2.10.1

# Password hashing - Primary: Argon2id (OWASP recommended)
argon2-cffi==23.1.0

# Password hashing - Legacy support
bcrypt>=4.2.0,<5.0.0

# FastAPI (pinned for consistency)
fastapi>=0.115.0,<1.0.0
starlette>=0.49.1

# Pydantic (pinned for consistency)
pydantic==2.12.5

# Redis for token revocation
redis[hiredis]>=7.1.0,<8.0.0

passlib[bcrypt]==1.7.4
