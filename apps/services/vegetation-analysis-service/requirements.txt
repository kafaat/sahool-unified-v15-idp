# Satellite Service - Requirements
# خدمة الأقمار الصناعية
# Updated: December 2025

# Web Framework
fastapi>=0.115.0,<1.0.0
starlette>=1.3.1
uvicorn[standard]>=0.34.0,<1.0.0
pydantic==2.13.1

# Authentication
PyJWT>=2.13.0,<3.0.0  # CVE-2024-53861
cryptography>=44.0.1,<47.0.0  # CVE-2024-12797, CVE-2026-26007  # CVE-2024-225, GHSA-3ww4-gg4f-jr7f, GHSA-9v9h-cgj8-h64p

# HTTP Client
httpx==0.28.1

# Logging
structlog>=24.1.0,<26.0.0

# Utilities
python-dotenv==1.2.2

# Redis cache
redis[hiredis]>=7.1.0,<8.0.0

# XML Security
defusedxml>=0.7.1,<1.0.0

# Metrics
prometheus-client==0.25.0

# ML/Scientific Computing
numpy>=1.26.0,<2.5.0

# Sentinel Hub / Earth Observation (optional — service falls back to simulated data if not installed)
# Install with: pip install sentinelhub>=3.10.0
sentinelhub>=3.10.0,<4.0.0
# Local EO package for SAHOOL satellite workflows
# Installed from packages/sahool-eo/ via pip install -e ../../packages/sahool-eo
# sahool-eo>=1.0.0  # uncomment in Dockerfile or install via: pip install -e ../../packages/sahool-eo

# Database (async PostgreSQL driver for scheduler)
asyncpg>=0.29.0,<0.32.0

# NATS (direct connection for field.created subscription)
nats-py>=2.6.0,<3.0.0

# Scheduler
apscheduler==3.11.2

# MinIO / S3-compatible object store (GeoTIFF + PNG asset upload)
boto3>=1.35.0,<2.0.0

# Retry with backoff for CDSE rate limits
tenacity>=8.2.0,<10.0.0

# Optional: faster TIFF parsing (falls back to tifffile if rasterio unavailable)
# rasterio>=1.3.0,<2.0.0
tifffile>=2023.1.1,<2025.0.0

# Optional: PNG thumbnail generation
Pillow>=10.0.0,<13.0.0
