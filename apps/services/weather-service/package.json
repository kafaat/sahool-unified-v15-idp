{
  "name": "sahool-weather-service",
  "version": "16.0.0",
  "description": "SAHOOL Weather Service - خدمة الطقس (Python/FastAPI)",
  "author": "SAHOOL Team",
  "private": true,
  "license": "MIT",
  "scripts": {
    "start": "echo 'This is a Python service. Use: uvicorn src.main:app --reload'",
    "start:dev": "cd src && uvicorn main:app --reload --port 8092",
    "test": "pytest tests/",
    "prisma:generate": "prisma generate",
    "prisma:push": "prisma db push",
    "prisma:migrate": "prisma migrate dev",
    "prisma:studio": "prisma studio"
  },
  "devDependencies": {
    "@prisma/client": "~5.22.0",
    "prisma": "~5.22.0"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "note": "This is a Python/FastAPI service. Node.js is only used for Prisma CLI."
}
