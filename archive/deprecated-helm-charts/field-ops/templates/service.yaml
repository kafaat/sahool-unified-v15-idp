apiVersion: v1
kind: Service
metadata:
  name: {{ include "field-ops.fullname" . }}
  labels:
    {{- include "field-ops.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
  - port: {{ .Values.service.port }}
    targetPort: http
    protocol: TCP
    name: http
  selector:
    {{- include "field-ops.selectorLabels" . | nindent 4 }}
