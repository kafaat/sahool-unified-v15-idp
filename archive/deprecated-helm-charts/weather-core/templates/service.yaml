apiVersion: v1
kind: Service
metadata:
  name: {{ include "weather-core.fullname" . }}
  labels:
    {{- include "weather-core.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
  - port: {{ .Values.service.port }}
    targetPort: http
    protocol: TCP
    name: http
  selector:
    {{- include "weather-core.selectorLabels" . | nindent 4 }}
