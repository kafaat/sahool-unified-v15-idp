apiVersion: v1
kind: Service
metadata:
  name: {{ include "yield-engine.fullname" . }}
  labels:
    {{- include "yield-engine.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
  - port: {{ .Values.service.port }}
    targetPort: http
    protocol: TCP
    name: http
  selector:
    {{- include "yield-engine.selectorLabels" . | nindent 4 }}
