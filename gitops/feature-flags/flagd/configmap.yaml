apiVersion: v1
kind: ConfigMap
metadata:
  name: flagd-config
  namespace: feature-flags
data:
  flags.json: |
    {
      "flags": {
        "ndvi.newPipeline": {
          "state": "ENABLED",
          "variants": { "on": true, "off": false },
          "defaultVariant": "off"
        },
        "advisor.promptV2": {
          "state": "ENABLED",
          "variants": { "v1": "v1", "v2": "v2" },
          "defaultVariant": "v1"
        }
      }
    }
