{{- if .Values.pdb.enabled }}
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: {{ include "advisory-service.fullname" . }}
  labels:
    {{- include "advisory-service.labels" . | nindent 4 }}
spec:
  minAvailable: {{ .Values.pdb.minAvailable }}
  selector:
    matchLabels:
      {{- include "advisory-service.selectorLabels" . | nindent 6 }}
{{- end }}
