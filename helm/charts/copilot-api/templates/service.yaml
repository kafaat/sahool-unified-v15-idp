apiVersion: v1
kind: Service
metadata:
  name: {{ include "copilot-api.fullname" . }}
  labels:
    {{- include "copilot-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "copilot-api.selectorLabels" . | nindent 4 }}
