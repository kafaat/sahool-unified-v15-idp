apiVersion: v1
kind: Service
metadata:
  name: {{ include "crop-intelligence-service.fullname" . }}
  labels:
    {{- include "crop-intelligence-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "crop-intelligence-service.selectorLabels" . | nindent 4 }}
