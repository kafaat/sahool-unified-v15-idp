apiVersion: v1
kind: Service
metadata:
  name: {{ include "indicators-service.fullname" . }}
  labels:
    {{- include "indicators-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "indicators-service.selectorLabels" . | nindent 4 }}
