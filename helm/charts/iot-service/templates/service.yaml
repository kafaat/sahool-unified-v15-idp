apiVersion: v1
kind: Service
metadata:
  name: {{ include "iot-service.fullname" . }}
  labels:
    {{- include "iot-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "iot-service.selectorLabels" . | nindent 4 }}
