apiVersion: v1
kind: Service
metadata:
  name: {{ include "vegetation-analysis-service.fullname" . }}
  labels:
    {{- include "vegetation-analysis-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "vegetation-analysis-service.selectorLabels" . | nindent 4 }}
