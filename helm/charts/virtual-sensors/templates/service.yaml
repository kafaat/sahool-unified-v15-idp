apiVersion: v1
kind: Service
metadata:
  name: {{ include "virtual-sensors.fullname" . }}
  labels:
    {{- include "virtual-sensors.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "virtual-sensors.selectorLabels" . | nindent 4 }}
