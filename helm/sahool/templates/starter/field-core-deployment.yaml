{{- /*
DEPRECATED: field-core has been consolidated into field-management-service.
This template is kept as a redirect — the actual deployment is handled by
deployments.yaml via .Values.services.fieldManagementService.

See: docs/migrations/FIELD_OPS_MIGRATION_SUMMARY.md
*/ -}}
