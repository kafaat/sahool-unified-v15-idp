apiVersion: v1
kind: Service
metadata:
  name: {{ include "agro-advisor.fullname" . }}
  labels:
    {{- include "agro-advisor.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
  - port: {{ .Values.service.port }}
    targetPort: http
    protocol: TCP
    name: http
  selector:
    {{- include "agro-advisor.selectorLabels" . | nindent 4 }}
