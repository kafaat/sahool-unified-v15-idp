apiVersion: v1
kind: Service
metadata:
  name: {{ include "billing-core.fullname" . }}
  labels:
    {{- include "billing-core.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
  - port: {{ .Values.service.port }}
    targetPort: http
    protocol: TCP
    name: http
  selector:
    {{- include "billing-core.selectorLabels" . | nindent 4 }}
