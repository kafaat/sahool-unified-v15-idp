apiVersion: v1
kind: Service
metadata:
  name: {{ include "edge-orchestrator-service.fullname" . }}
  labels:
    {{- include "edge-orchestrator-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "edge-orchestrator-service.selectorLabels" . | nindent 4 }}
