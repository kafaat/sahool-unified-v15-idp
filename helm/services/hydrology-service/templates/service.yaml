apiVersion: v1
kind: Service
metadata:
  name: {{ include "hydrology-service.fullname" . }}
  labels:
    {{- include "hydrology-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "hydrology-service.selectorLabels" . | nindent 4 }}
