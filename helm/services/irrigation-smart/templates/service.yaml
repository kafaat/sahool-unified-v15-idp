apiVersion: v1
kind: Service
metadata:
  name: {{ include "irrigation-smart.fullname" . }}
  labels:
    {{- include "irrigation-smart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
  - port: {{ .Values.service.port }}
    targetPort: http
    protocol: TCP
    name: http
  selector:
    {{- include "irrigation-smart.selectorLabels" . | nindent 4 }}
