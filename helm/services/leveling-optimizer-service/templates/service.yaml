apiVersion: v1
kind: Service
metadata:
  name: {{ include "leveling-optimizer-service.fullname" . }}
  labels:
    {{- include "leveling-optimizer-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "leveling-optimizer-service.selectorLabels" . | nindent 4 }}
