apiVersion: v1
kind: Service
metadata:
  name: {{ include "ndvi-engine.fullname" . }}
  labels:
    {{- include "ndvi-engine.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
  - port: {{ .Values.service.port }}
    targetPort: http
    protocol: TCP
    name: http
  selector:
    {{- include "ndvi-engine.selectorLabels" . | nindent 4 }}
