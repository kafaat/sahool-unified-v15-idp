apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "yolo26-vision-service.fullname" . }}
  labels:
    {{- include "yolo26-vision-service.labels" . | nindent 4 }}
spec:
  {{- if not .Values.autoscaling.enabled }}
  replicas: {{ .Values.replicaCount }}
  {{- end }}
  selector:
    matchLabels:
      {{- include "yolo26-vision-service.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      annotations:
        {{- with .Values.podAnnotations }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
      labels:
        {{- include "yolo26-vision-service.selectorLabels" . | nindent 8 }}
        environment: {{ .Values.environment }}
        deploymentSlot: {{ .Values.deploymentSlot }}
    spec:
      terminationGracePeriodSeconds: 60
      automountServiceAccountToken: false
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      serviceAccountName: {{ include "yolo26-vision-service.serviceAccountName" . }}
      securityContext:
        {{- toYaml .Values.podSecurityContext | nindent 8 }}
      containers:
        - name: {{ .Chart.Name }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - name: http
              containerPort: 8150
              protocol: TCP
          env:
            - name: ENVIRONMENT
              value: {{ .Values.environment | quote }}
            - name: DEPLOYMENT_SLOT
              value: {{ .Values.deploymentSlot | quote }}
            - name: SERVICE_PORT
              value: "8150"
            {{- range $key, $value := .Values.env }}
            - name: {{ $key }}
              value: {{ $value | quote }}
            {{- end }}
            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: {{ include "yolo26-vision-service.fullname" . }}-secrets
                  key: jwt-secret
                  optional: true
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: {{ include "yolo26-vision-service.fullname" . }}-secrets
                  key: database-url
                  optional: true
            - name: NATS_URL
              valueFrom:
                configMapKeyRef:
                  name: {{ include "yolo26-vision-service.fullname" . }}-config
                  key: NATS_URL
                  optional: true
            - name: REDIS_URL
              valueFrom:
                configMapKeyRef:
                  name: {{ include "yolo26-vision-service.fullname" . }}-config
                  key: REDIS_URL
                  optional: true
            {{- if .Values.gpu.enabled }}
            - name: NVIDIA_VISIBLE_DEVICES
              value: "all"
            - name: NVIDIA_DRIVER_CAPABILITIES
              value: "compute,utility"
            {{- end }}
          livenessProbe:
            httpGet:
              path: {{ .Values.probes.liveness.path }}
              port: http
            initialDelaySeconds: {{ .Values.probes.liveness.initialDelaySeconds }}
            periodSeconds: {{ .Values.probes.liveness.periodSeconds }}
            timeoutSeconds: {{ .Values.probes.liveness.timeoutSeconds }}
            failureThreshold: {{ .Values.probes.liveness.failureThreshold }}
          readinessProbe:
            httpGet:
              path: {{ .Values.probes.readiness.path }}
              port: http
            initialDelaySeconds: {{ .Values.probes.readiness.initialDelaySeconds }}
            periodSeconds: {{ .Values.probes.readiness.periodSeconds }}
            timeoutSeconds: {{ .Values.probes.readiness.timeoutSeconds }}
            failureThreshold: {{ .Values.probes.readiness.failureThreshold }}
          lifecycle:
            preStop:
              exec:
                command: ["/bin/sh", "-c", "sleep 15"]
          resources:
            {{- if .Values.gpu.enabled }}
            {{- toYaml .Values.resources | nindent 12 }}
            {{- else }}
            {{- toYaml .Values.resourcesNonGpu | nindent 12 }}
            {{- end }}
          securityContext:
            {{- toYaml .Values.securityContext | nindent 12 }}
          volumeMounts:
            {{- if .Values.modelStorage.enabled }}
            - name: model-storage
              mountPath: /models
            {{- end }}
            - name: tmp
              mountPath: /tmp
            - name: cache
              mountPath: /.cache
      volumes:
        {{- if .Values.modelStorage.enabled }}
        - name: model-storage
          persistentVolumeClaim:
            claimName: {{ include "yolo26-vision-service.fullname" . }}-models
        {{- end }}
        - name: tmp
          emptyDir: {}
        - name: cache
          emptyDir: {}
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
