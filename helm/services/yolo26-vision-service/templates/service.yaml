apiVersion: v1
kind: Service
metadata:
  name: {{ include "yolo26-vision-service.fullname" . }}
  labels:
    {{- include "yolo26-vision-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "yolo26-vision-service.selectorLabels" . | nindent 4 }}
