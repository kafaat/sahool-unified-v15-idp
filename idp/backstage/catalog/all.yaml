apiVersion: backstage.io/v1alpha1
kind: Location
metadata:
  name: sahool-catalog
spec:
  targets:
    - ./systems/sahool-system.yaml
    - ./templates/templates.yaml
    # Hand-written entities for vision/terrain/edge services + skills library
    - ../../catalog/all.yaml
    # Auto-generated entities for the remaining 82 services in governance/services.yaml.
    # Regenerate with: python3 scripts/generators/generate_backstage_catalog.py
    - ./services-location.yaml
