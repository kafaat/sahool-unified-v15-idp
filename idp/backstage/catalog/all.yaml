apiVersion: backstage.io/v1alpha1
kind: Location
metadata:
  name: sahool-catalog
spec:
  targets:
    - ./systems/sahool-system.yaml
    - ./templates/templates.yaml
    # New services catalog (YOLO26 Vision, Terrain, Hydrology, Leveling, Edge)
    - ../../catalog/all.yaml
