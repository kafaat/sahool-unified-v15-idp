# ═══════════════════════════════════════════════════════════════════════════════
# SAHOOL Deployment Packages - Environment Configuration
# حزم نشر سهول - إعدادات البيئة
#
# Copy this file to .env in your package directory (starter/professional/enterprise)
# and fill in your values
# ═══════════════════════════════════════════════════════════════════════════════

# ─────────────────────────────────────────────────────────────────────────────
# Environment Settings
# ─────────────────────────────────────────────────────────────────────────────
ENVIRONMENT=development
LOG_LEVEL=INFO

# ─────────────────────────────────────────────────────────────────────────────
# PostgreSQL Database (REQUIRED for all packages)
# قاعدة البيانات PostgreSQL (مطلوبة لجميع الحزم)
# ─────────────────────────────────────────────────────────────────────────────
POSTGRES_USER=sahool
POSTGRES_PASSWORD=your_secure_postgres_password_change_me
POSTGRES_DB=sahool

# ─────────────────────────────────────────────────────────────────────────────
# Redis Cache (REQUIRED for all packages)
# ذاكرة التخزين المؤقت Redis (مطلوبة لجميع الحزم)
# ─────────────────────────────────────────────────────────────────────────────
REDIS_PASSWORD=your_secure_redis_password_change_me

# ─────────────────────────────────────────────────────────────────────────────
# JWT Authentication (REQUIRED for all packages)
# المصادقة JWT (مطلوبة لجميع الحزم)
# ─────────────────────────────────────────────────────────────────────────────
# Generate a secure random key (at least 32 characters)
# openssl rand -base64 32
JWT_SECRET_KEY=your_jwt_secret_key_at_least_32_characters_change_me
JWT_ALGORITHM=HS256

# ─────────────────────────────────────────────────────────────────────────────
# SMTP Configuration (Optional - for email notifications)
# إعدادات SMTP (اختياري - للإشعارات عبر البريد الإلكتروني)
# ─────────────────────────────────────────────────────────────────────────────
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM_EMAIL=noreply@sahool.com

# ─────────────────────────────────────────────────────────────────────────────
# Weather API Configuration (Optional - Starter, Professional, Enterprise)
# إعدادات API الطقس (اختياري)
#
# Free providers are used by default (Open-Meteo)
# Add these keys for additional providers and redundancy
# ─────────────────────────────────────────────────────────────────────────────
USE_MULTI_PROVIDER=true
USE_MOCK_WEATHER=false

# OpenWeatherMap (Optional - Free tier: 1,000 calls/day)
# https://openweathermap.org/api
OPENWEATHERMAP_API_KEY=

# WeatherAPI (Optional - Free tier: 1,000,000 calls/month)
# https://www.weatherapi.com/
WEATHERAPI_KEY=

# ─────────────────────────────────────────────────────────────────────────────
# Satellite Imagery Providers (Professional & Enterprise packages only)
# مزودو صور الأقمار الصناعية (للحزمة الاحترافية والمؤسسات فقط)
#
# Free providers are used by default (Copernicus STAC)
# ─────────────────────────────────────────────────────────────────────────────
# Sentinel Hub (ESA Copernicus - Free tier: 30,000 processing units/month)
# Register at: https://www.sentinel-hub.com/
SENTINEL_HUB_CLIENT_ID=
SENTINEL_HUB_CLIENT_SECRET=

# NASA Earthdata (Free - MODIS/VIIRS data)
# Register at: https://urs.earthdata.nasa.gov/
NASA_EARTHDATA_USERNAME=
NASA_EARTHDATA_PASSWORD=

# Planet Labs (Enterprise only - Paid high-resolution imagery)
PLANET_API_KEY=

# ─────────────────────────────────────────────────────────────────────────────
# IoT Configuration (Enterprise package only)
# إعدادات إنترنت الأشياء (لحزمة المؤسسات فقط)
# ─────────────────────────────────────────────────────────────────────────────
MQTT_USER=sahool_iot
MQTT_PASSWORD=your_secure_mqtt_password_change_me

# ─────────────────────────────────────────────────────────────────────────────
# AI Services Configuration (Enterprise package only)
# إعدادات خدمات الذكاء الاصطناعي (لحزمة المؤسسات فقط)
# ─────────────────────────────────────────────────────────────────────────────
# Multi-Provider LLM Configuration
USE_MULTI_PROVIDER=true
PRIMARY_LLM_PROVIDER=anthropic

# Anthropic Claude (Recommended for Arabic support)
# Get your API key at: https://console.anthropic.com/
ANTHROPIC_API_KEY=
CLAUDE_MODEL=claude-3-5-sonnet-20241022

# OpenAI GPT (Fallback)
# Get your API key at: https://platform.openai.com/
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o

# Google Gemini (Optional)
# Get your API key at: https://makersuite.google.com/app/apikey
GOOGLE_API_KEY=
GEMINI_MODEL=gemini-1.5-pro

# ─────────────────────────────────────────────────────────────────────────────
# Payment Gateway Configuration (Enterprise package only)
# إعدادات بوابات الدفع (لحزمة المؤسسات فقط)
# ─────────────────────────────────────────────────────────────────────────────
# Stripe (International payments)
STRIPE_API_KEY=
STRIPE_WEBHOOK_SECRET=

# Tharwatt (MENA region - Yemen)
THARWATT_BASE_URL=https://developers-test.tharwatt.com:5253
THARWATT_API_KEY=
THARWATT_MERCHANT_ID=
THARWATT_WEBHOOK_SECRET=

# ─────────────────────────────────────────────────────────────────────────────
# Monitoring Stack (Enterprise package only)
# حزمة المراقبة (لحزمة المؤسسات فقط)
# ─────────────────────────────────────────────────────────────────────────────
# Grafana Admin Credentials
GRAFANA_ADMIN_USER=admin
GRAFANA_ADMIN_PASSWORD=your_secure_grafana_password_change_me
GRAFANA_ROOT_URL=http://localhost:3002

# ─────────────────────────────────────────────────────────────────────────────
# Push Notifications (Optional - for mobile apps)
# الإشعارات الفورية (اختياري - للتطبيقات المحمولة)
# ─────────────────────────────────────────────────────────────────────────────
# Firebase Cloud Messaging
FCM_SERVER_KEY=

# ─────────────────────────────────────────────────────────────────────────────
# CORS Configuration
# إعدادات CORS
# ─────────────────────────────────────────────────────────────────────────────
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:8080

# ═══════════════════════════════════════════════════════════════════════════════
# Package-Specific Notes
# ملاحظات خاصة بالحزم
# ═══════════════════════════════════════════════════════════════════════════════
#
# STARTER PACKAGE (حزمة المبتدئين):
#   - Required: POSTGRES_*, REDIS_PASSWORD, JWT_SECRET_KEY
#   - Optional: SMTP_*, Weather APIs
#
# PROFESSIONAL PACKAGE (الحزمة الاحترافية):
#   - Required: All Starter requirements
#   - Optional: Satellite imagery APIs (SENTINEL_HUB_*, NASA_EARTHDATA_*)
#
# ENTERPRISE PACKAGE (حزمة المؤسسات):
#   - Required: All Professional requirements + MQTT_PASSWORD + GRAFANA_ADMIN_PASSWORD
#   - Recommended: AI APIs (ANTHROPIC_API_KEY or OPENAI_API_KEY)
#   - Optional: Payment gateways, FCM
#
# ═══════════════════════════════════════════════════════════════════════════════

# ─────────────────────────────────────────────────────────────────────────────
# Security Best Practices | أفضل ممارسات الأمان
# ─────────────────────────────────────────────────────────────────────────────
# 1. NEVER commit this file with real credentials to version control
#    لا تقم أبداً بإرسال هذا الملف مع بيانات الاعتماد الحقيقية إلى نظام التحكم بالإصدارات
#
# 2. Use strong, unique passwords for each service
#    استخدم كلمات مرور قوية وفريدة لكل خدمة
#
# 3. Generate random keys using: openssl rand -base64 32
#    قم بإنشاء مفاتيح عشوائية باستخدام: openssl rand -base64 32
#
# 4. Rotate secrets regularly in production
#    قم بتدوير الأسرار بانتظام في بيئة الإنتاج
#
# 5. Use environment-specific .env files (.env.dev, .env.prod)
#    استخدم ملفات .env خاصة بكل بيئة
#
# 6. Consider using secret management tools (HashiCorp Vault, AWS Secrets Manager)
#    فكر في استخدام أدوات إدارة الأسرار
# ─────────────────────────────────────────────────────────────────────────────
