# ═══════════════════════════════════════════════════════════════════════════════
# SAHOOL Platform - Development & Diagnostic Tools
# أدوات التطوير والتشخيص لمنصة سهول
# ═══════════════════════════════════════════════════════════════════════════════

# Linting & Formatting
ruff>=0.8.0                    # Fast Python linter and formatter
pyright>=1.1.390               # Static type checker (better than mypy)
pre-commit>=4.0.0              # Git hook management

# Security Analysis
bandit>=1.7.10                 # Security vulnerability scanner
safety>=3.2.0                  # Check dependencies for vulnerabilities

# Code Quality
vulture>=2.14                  # Find dead/unused code
radon>=6.0.1                   # Code complexity analyzer
xenon>=0.9.3                   # Monitor code complexity thresholds

# Testing Tools
pytest>=8.3.0                  # Testing framework
pytest-cov>=6.0.0              # Coverage reporting
pytest-asyncio==0.26.0         # Async test support
pytest-xdist>=3.5.0            # Parallel test execution
pytest-mock>=3.11.0            # Mock support
sqlalchemy[asyncio]>=2.0.0     # SQLAlchemy with async support (needed by shared/libs/database.py tests)
httpx>=0.24.0                  # HTTP client (needed by FastAPI TestClient)
cffi>=1.15.0                   # C FFI (needed by cryptography/PyJWT)

# Documentation
pydoc-markdown>=4.8.0          # Generate markdown docs

# Profiling
py-spy>=0.4.0                  # Sampling profiler
memray>=1.14.0                 # Memory profiler (Linux only)
scalene>=1.5.0                 # CPU/Memory/GPU profiler

# Database Tools
sqlfluff>=3.2.0                # SQL linter and formatter

# Docker/Container Tools
# Note: docker-compose linting is handled by hadolint (standalone binary) and
# the CI workflow uses hadolint/hadolint-action@v3.3.0 directly.
