# SAHOOL Base Requirements
# ========================
# Core dependencies shared across ALL Python microservices
# Updated: March 2026
# Synced with constraints.txt
# Version: 16.0.0

# FastAPI Framework Stack
fastapi==0.135.2
uvicorn[standard]==0.42.0
pydantic==2.12.5

# Async HTTP Client
httpx==0.28.1

# File Uploads (required by FastAPI for multipart form data)
python-multipart>=0.0.12  # Constrained to ==0.0.22 by constraints.txt

# Event Contract Validation (shared.contracts.events)
jsonschema>=4.20.0

# Redis Client (shared.cache)
redis>=7.1.0,<8.0.0

# Environment & Configuration
python-dotenv==1.2.2
