# SAHOOL Infrastructure Requirements
# ===================================
# Shared infrastructure dependencies for services with data/messaging needs
# Updated: March 2026
# Synced with constraints.txt

# Include base requirements
-r base.txt

# Database & ORM
asyncpg==0.31.0
tortoise-orm==1.1.7
# aerich removed: 0.9.2 requires tortoise-orm<1.0.0, incompatible with 1.1.6

# NATS Messaging
nats-py==2.14.0

# Redis Caching
redis>=7.1.0,<8.0.0

# Authentication & Security
python-jose[cryptography]>=3.5.0  # CVE-2024-33663, CVE-2024-33664
PyJWT>=2.10.1,<3.0.0  # CVE-2024-53861
passlib[bcrypt]==1.7.4
bcrypt>=4.2.0,<5.0.0  # Explicit install to avoid PBKDF2 fallback

# Two-Factor Authentication (2FA)
pyotp>=2.9.0,<3.0.0          # TOTP generation and verification
qrcode[pil]>=7.4.2,<8.0.0    # QR code generation with PIL support
