This folder is intentionally minimal.

Recommended:
- Install monitoring stack using `k8s/addons/install-autoscaling-observability.sh`
- Or replace this folder with your preferred GitOps-managed Helm releases (kube-prometheus-stack, prometheus-adapter).
